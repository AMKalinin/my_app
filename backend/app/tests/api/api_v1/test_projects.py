from fastapi.testclient import TestClient
# from sqlalchemy.orm import Session


def test_read_main() -> None:
    # response = client.get('api/v1/projects/')
    # assert response.status_code == 200
    # assert response.json() == {'stroka': 'ok'}
    assert 2 == 2
    assert 2 == 3